{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://easydora.internal/schemas/product-updated.schema.json",
  "title": "product.updated",
  "description": "Published by products-service (RabbitMQ product.exchange, routing key product.updated). Consumed by inventory-service. Same price precision caveat as product.created (BigDecimal vs float64, not caught by JSON Schema's number type).",
  "type": "object",
  "properties": {
    "productId": { "type": "string" },
    "productName": { "type": "string" },
    "price": { "type": "number" },
    "active": { "type": "boolean" },
    "updatedAt": { "type": "string" }
  },
  "required": ["productId", "productName", "price", "active", "updatedAt"]
}
