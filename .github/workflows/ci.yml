name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  gateway:
    name: api-gateway (Go)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: api-gateway/go.sum
      - name: go vet
        working-directory: api-gateway
        run: go vet ./...
      - name: go build
        working-directory: api-gateway
        run: go build ./...
      - name: go test -race
        working-directory: api-gateway
        run: go test -race ./...

  inventory:
    name: inventory-service (Go)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: inventory-service/go.sum
      - name: go vet
        working-directory: inventory-service
        run: go vet ./...
      - name: go build
        working-directory: inventory-service
        run: go build ./...
      - name: go test -race
        working-directory: inventory-service
        run: go test -race ./...

  spring-build:
    name: ${{ matrix.service }} (mvn test)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [auth-service, products-service, orders-service, billing-service]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: install correlation-commons
        run: |
          mvn install -N
          cd correlation-commons && mvn install -DskipTests
      - name: mvn test
        working-directory: ${{ matrix.service }}
        run: mvn test

  notification:
    name: notification-service (Python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: install dependencies
        working-directory: notification-service
        run: pip install -r requirements.txt
      - name: pytest (unit only, no live infra)
        working-directory: notification-service
        run: pytest -m "not integration"

  # Phase 2: real-infrastructure integration tests (wiring/Outbox), gated on
  # Phase 1 passing first so a broken unit test fails fast without spending
  # time provisioning service containers. A single job with a mixed
  # java/go matrix, rather than one job per language, so the Postgres/
  # RabbitMQ services block — and the credentials
  # (local_dev_placeholder) each service's application-dev.properties /
  # config.Load() already default to, so no secrets are needed — is
  # written once; each matrix entry still gets its own fresh pair of
  # containers (GitHub Actions has no way to share service containers
  # across jobs, but a matrix only fans out one job definition, so this
  # avoids repeating the YAML for it).
  integration:
    name: ${{ matrix.name }} (real infra)
    runs-on: ubuntu-latest
    needs: [gateway, inventory, spring-build]
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: auth-service
            language: java
          - name: orders-service
            language: java
          - name: billing-service
            language: java
          - name: inventory-service
            language: go
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: easydora
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: local_dev_placeholder
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U admin -d easydora"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: admin
          RABBITMQ_DEFAULT_PASS: local_dev_placeholder
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - if: matrix.language == 'java'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - if: matrix.language == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: inventory-service/go.sum
      - if: matrix.language == 'java'
        name: install correlation-commons
        run: |
          mvn install -N
          cd correlation-commons && mvn install -DskipTests
      - if: matrix.language == 'java'
        name: mvn verify
        working-directory: ${{ matrix.name }}
        run: mvn verify
      - if: matrix.language == 'go'
        name: go test -tags=integration
        working-directory: ${{ matrix.name }}
        env:
          DB_HOST: localhost
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_HOST: localhost
          RABBITMQ_PASSWORD: local_dev_placeholder
        run: go test -tags=integration ./...

  # Owns and automates the empirical confirmation ADR-0038 recorded
  # manually: that the four Spring services tolerate Postgres/RabbitMQ not
  # being ready yet at boot, instead of crashing on the first connection
  # attempt. Unlike every other job here, this one deliberately does NOT
  # use a `services:` block -- those start before any step runs, which
  # would remove the exact race this job exists to reproduce. Postgres and
  # RabbitMQ are started manually, in a later step, only once the app has
  # already been running against neither for a while.
  boot-race:
    name: ${{ matrix.service }} (Postgres/RabbitMQ startup race)
    runs-on: ubuntu-latest
    needs: [spring-build]
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: auth-service
            port: 8081
            context_path: /auth
          - service: products-service
            port: 8082
            context_path: /products
          - service: orders-service
            port: 8084
            context_path: /orders
          - service: billing-service
            port: 8085
            context_path: /billing
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: install correlation-commons
        run: |
          mvn install -N
          cd correlation-commons && mvn install -DskipTests
      - name: package ${{ matrix.service }}
        working-directory: ${{ matrix.service }}
        run: mvn -DskipTests package

      - name: start ${{ matrix.service }} with neither Postgres nor RabbitMQ reachable yet
        working-directory: ${{ matrix.service }}
        env:
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_PASSWORD: local_dev_placeholder
          SPRING_PROFILES_ACTIVE: dev
          APP_JWT_SECRET: ci_pipeline_placeholder_app_secret_do_not_use_in_prod
        run: |
          JAR=$(ls target/*.jar | grep -v original- | head -n1)
          nohup java -jar "$JAR" > /tmp/boot-race.log 2>&1 &
          echo $! > /tmp/boot-race.pid

      - name: confirm the process survives the race instead of crashing immediately
        run: |
          sleep 15
          PID=$(cat /tmp/boot-race.pid)
          if ! kill -0 "$PID" 2>/dev/null; then
            echo "process exited before Postgres/RabbitMQ ever started -- boot-race tolerance regressed"
            cat /tmp/boot-race.log
            exit 1
          fi
          echo "still running 15s into a Postgres/RabbitMQ-less boot, as expected"

      - name: start Postgres and RabbitMQ only now, deliberately late
        run: |
          docker run -d --name ci-postgres -p 5432:5432 \
            -e POSTGRES_DB=easydora -e POSTGRES_USER=admin -e POSTGRES_PASSWORD=local_dev_placeholder \
            postgres:15-alpine
          docker run -d --name ci-rabbitmq -p 5672:5672 \
            -e RABBITMQ_DEFAULT_USER=admin -e RABBITMQ_DEFAULT_PASS=local_dev_placeholder \
            rabbitmq:3-management-alpine

      - name: wait for the service to recover once real infra shows up
        run: |
          for i in $(seq 1 60); do
            if curl -sf "http://localhost:${{ matrix.port }}${{ matrix.context_path }}/health" > /dev/null; then
              echo "recovered and became healthy"
              exit 0
            fi
            sleep 2
          done
          echo "never recovered within 120s of Postgres/RabbitMQ becoming available"
          cat /tmp/boot-race.log
          exit 1

      - name: dump service log on failure
        if: failure()
        run: cat /tmp/boot-race.log || true

  # Phase 3: cross-service e2e (ADR-0013). Unlike Phase 2 -- which runs each
  # service's own test suite against a real broker/DB but always in
  # isolation, one service per job -- this starts multiple real services as
  # actual running processes against the *same* Postgres/RabbitMQ pair and
  # drives the flow through their public HTTP APIs only, so the message a
  # consumer reacts to is never hand-built, it's whatever the producing
  # service's real code actually put on the wire. Gated on Phase 2 passing
  # first for the same fail-fast reasoning ADR-0012 already applies.
  e2e-catalog-onboarding:
    name: catalog onboarding (cross-service e2e)
    runs-on: ubuntu-latest
    needs: [integration]
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: easydora
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: local_dev_placeholder
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U admin -d easydora"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: admin
          RABBITMQ_DEFAULT_PASS: local_dev_placeholder
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: inventory-service/go.sum

      - name: install correlation-commons
        run: |
          mvn install -N
          cd correlation-commons && mvn install -DskipTests
      - name: package auth-service
        working-directory: auth-service
        run: mvn -DskipTests package
      - name: package products-service
        working-directory: products-service
        run: mvn -DskipTests package
      - name: build inventory-service
        working-directory: inventory-service
        run: go build -o inventory-service-bin .

      - name: start auth-service
        working-directory: auth-service
        env:
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_PASSWORD: local_dev_placeholder
          SPRING_PROFILES_ACTIVE: dev
          APP_JWT_SECRET: ci_pipeline_placeholder_app_secret_do_not_use_in_prod
        run: |
          JAR=$(ls target/*.jar | grep -v original- | head -n1)
          nohup java -jar "$JAR" > /tmp/auth-service.log 2>&1 &
      - name: start products-service
        working-directory: products-service
        env:
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_PASSWORD: local_dev_placeholder
          SPRING_PROFILES_ACTIVE: dev
        run: |
          JAR=$(ls target/*.jar | grep -v original- | head -n1)
          nohup java -jar "$JAR" > /tmp/products-service.log 2>&1 &
      - name: start inventory-service
        working-directory: inventory-service
        env:
          DB_HOST: localhost
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_HOST: localhost
          RABBITMQ_PASSWORD: local_dev_placeholder
        run: nohup ./inventory-service-bin > /tmp/inventory-service.log 2>&1 &

      - name: wait for services to be healthy
        run: |
          for url in http://localhost:8081/auth/health http://localhost:8082/products/health http://localhost:8083/inventory/health; do
            for i in $(seq 1 60); do
              if curl -sf "$url" > /dev/null; then
                echo "$url is healthy"
                break
              fi
              if [ "$i" -eq 60 ]; then
                echo "$url never became healthy after 60s"
                exit 1
              fi
              sleep 1
            done
          done

      - name: run cross-service e2e tests
        working-directory: e2e-tests
        run: mvn test -Dtest=CatalogOnboardingE2ETest

      - name: dump service logs on failure
        if: failure()
        run: |
          echo "--- auth-service ---"; cat /tmp/auth-service.log || true
          echo "--- products-service ---"; cat /tmp/products-service.log || true
          echo "--- inventory-service ---"; cat /tmp/inventory-service.log || true

  e2e-order-lifecycle:
    name: order lifecycle (cross-service e2e)
    runs-on: ubuntu-latest
    needs: [integration]
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: easydora
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: local_dev_placeholder
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U admin -d easydora"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: admin
          RABBITMQ_DEFAULT_PASS: local_dev_placeholder
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache-dependency-path: inventory-service/go.sum

      - name: install correlation-commons
        run: |
          mvn install -N
          cd correlation-commons && mvn install -DskipTests
      - name: package auth-service
        working-directory: auth-service
        run: mvn -DskipTests package
      - name: package orders-service
        working-directory: orders-service
        run: mvn -DskipTests package
      - name: package billing-service
        working-directory: billing-service
        run: mvn -DskipTests package
      - name: build inventory-service
        working-directory: inventory-service
        run: go build -o inventory-service-bin .

      - name: start auth-service
        working-directory: auth-service
        env:
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_PASSWORD: local_dev_placeholder
          SPRING_PROFILES_ACTIVE: dev
          APP_JWT_SECRET: ci_pipeline_placeholder_app_secret_do_not_use_in_prod
        run: |
          JAR=$(ls target/*.jar | grep -v original- | head -n1)
          nohup java -jar "$JAR" > /tmp/auth-service.log 2>&1 &
      - name: start orders-service
        working-directory: orders-service
        env:
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_PASSWORD: local_dev_placeholder
          SPRING_PROFILES_ACTIVE: dev
        run: |
          JAR=$(ls target/*.jar | grep -v original- | head -n1)
          nohup java -jar "$JAR" > /tmp/orders-service.log 2>&1 &
      - name: start billing-service
        working-directory: billing-service
        env:
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_PASSWORD: local_dev_placeholder
          SPRING_PROFILES_ACTIVE: dev
        run: |
          JAR=$(ls target/*.jar | grep -v original- | head -n1)
          nohup java -jar "$JAR" > /tmp/billing-service.log 2>&1 &
      - name: start inventory-service
        working-directory: inventory-service
        env:
          DB_HOST: localhost
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_HOST: localhost
          RABBITMQ_PASSWORD: local_dev_placeholder
        run: nohup ./inventory-service-bin > /tmp/inventory-service.log 2>&1 &

      - name: wait for services to be healthy
        run: |
          for url in http://localhost:8081/auth/health http://localhost:8083/inventory/health http://localhost:8084/orders/health http://localhost:8085/billing/health; do
            for i in $(seq 1 60); do
              if curl -sf "$url" > /dev/null; then
                echo "$url is healthy"
                break
              fi
              if [ "$i" -eq 60 ]; then
                echo "$url never became healthy after 60s"
                exit 1
              fi
              sleep 1
            done
          done

      - name: run cross-service e2e tests
        working-directory: e2e-tests
        run: mvn test -Dtest=OrderLifecycleE2ETest

      - name: dump service logs on failure
        if: failure()
        run: |
          echo "--- auth-service ---"; cat /tmp/auth-service.log || true
          echo "--- orders-service ---"; cat /tmp/orders-service.log || true
          echo "--- billing-service ---"; cat /tmp/billing-service.log || true
          echo "--- inventory-service ---"; cat /tmp/inventory-service.log || true

  e2e-notification-flow:
    name: notification flow (cross-service e2e)
    runs-on: ubuntu-latest
    needs: [integration, notification]
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: easydora
          POSTGRES_USER: admin
          POSTGRES_PASSWORD: local_dev_placeholder
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U admin -d easydora"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      rabbitmq:
        image: rabbitmq:3-management-alpine
        env:
          RABBITMQ_DEFAULT_USER: admin
          RABBITMQ_DEFAULT_PASS: local_dev_placeholder
        ports:
          - 5672:5672
          - 15672:15672
        options: >-
          --health-cmd "rabbitmq-diagnostics ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: install correlation-commons
        run: |
          mvn install -N
          cd correlation-commons && mvn install -DskipTests
      - name: package auth-service
        working-directory: auth-service
        run: mvn -DskipTests package
      - name: install notification-service dependencies
        working-directory: notification-service
        run: pip install -r requirements.txt

      - name: start auth-service
        working-directory: auth-service
        env:
          DB_PASSWORD: local_dev_placeholder
          RABBITMQ_PASSWORD: local_dev_placeholder
          SPRING_PROFILES_ACTIVE: dev
          APP_JWT_SECRET: ci_pipeline_placeholder_app_secret_do_not_use_in_prod
        run: |
          JAR=$(ls target/*.jar | grep -v original- | head -n1)
          nohup java -jar "$JAR" > /tmp/auth-service.log 2>&1 &

      - name: wait for auth-service to be healthy
        run: |
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8081/auth/health > /dev/null; then
              echo "auth-service is healthy"
              break
            fi
            if [ "$i" -eq 60 ]; then
              echo "auth-service never became healthy after 60s"
              exit 1
            fi
            sleep 1
          done

      - name: run notification-service integration tests
        working-directory: notification-service
        env:
          AUTH_SERVICE_URL: http://localhost:8081
          RABBITMQ_URL: amqp://admin:local_dev_placeholder@localhost:5672
          DB_HOST: localhost
          DB_PASSWORD: local_dev_placeholder
        run: pytest -m integration

      - name: dump service logs on failure
        if: failure()
        run: |
          echo "--- auth-service ---"; cat /tmp/auth-service.log || true
