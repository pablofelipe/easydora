# Named "prometheus" -- the exact hostname Grafana's datasource
# provisioning file already points at (see docs/adr/0040).
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: easydora
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
